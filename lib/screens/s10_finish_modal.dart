import 'package:flutter/material.dart';
import '../theme/theme.dart';

class S10FinishModal extends StatelessWidget {
  const S10FinishModal({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('S10FinishModal'),
      ),
      body: const Center(
        child: Text('S10FinishModal'),
      ),
    );
  }
}
